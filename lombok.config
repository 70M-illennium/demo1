# Stop Lombok looking for config files further up the filesystem.
config.stopBubbling = true

# Copy these annotations from a field onto the parameter that @RequiredArgsConstructor
# generates for it, so constructor injection still sees @Qualifier.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
